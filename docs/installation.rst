============
Installation
============

At the command line::

    $ easy_install whatthewhat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv whatthewhat
    $ pip install whatthewhat